import OP from '../definitions/operations';
import STAT from '../definitions/statuses';

const initState = {
  //--- TODO
};

const scopeReducer = (state = initState, action) => {
  //--- TODO
  return state;
};

export default scopeReducer;
